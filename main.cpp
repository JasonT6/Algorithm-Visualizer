#include <iostream>
#include <vector>
#include "node.hpp"

using namespace std;

int main(){
    cout << "it works" << endl;

    // TESTCODE
    // node head = node(nullptr, nullptr, nullptr, nullptr, new x_y_position(10, 10));
    // head.east = new node(nullptr, nullptr, nullptr, nullptr, new x_y_position(20, 10));
    // cout << head.position->getX() << endl;
    // cout << head.east->position->getX() << endl;
    // head.east->position->setX(100);
    // cout << head.east->position->getX();
    return 0;
}